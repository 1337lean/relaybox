# Contributing

Issues and focused pull requests are welcome. Before submitting, run `gofmt -w .`, `go vet ./...`, `go test ./...`, and `go test -race ./...`. Add tests for behavior changes, keep the runtime dependency-free, preserve the append-only format, and update operational or security documentation when applicable. By contributing, you agree that your work is licensed under MIT.
